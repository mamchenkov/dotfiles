# Follow symbolic links
--follow

# Always sort the files
--sort-files

# Use smart case (case-insensitive when pattern has no uppercase letters)
--smart-case

# Add .ctp (CakePHP view templates) to PHP type
--type-add=php=.ctp

# Ignore common directories
--ignore-dir=.git
--ignore-dir=build
--ignore-dir=cache
